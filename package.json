{
  "name": "update-flat-package-tree",
  "description": "update a flat package dependency tree manifest for a new package version",
  "version": "1.1.0",
  "author": "Kyle E. Mitchell <kyle@kemitchell.com> (https://kemitchell.com/)",
  "dependencies": {
    "array-find": "^1.0.0",
    "merge-flat-package-trees": "^1.0.0",
    "semver": "^5.3.0"
  },
  "devDependencies": {
    "istanbul": "^0.4.4",
    "sort-flat-package-tree": "^1.0.0",
    "standard": "^7.1.2",
    "tape": "^4.6.0"
  },
  "files": [
    "index.js"
  ],
  "license": "MIT",
  "repository": "kemitchell/update-flat-package-tree.js",
  "scripts": {
    "precoverage": "istanbul cover -- test.js",
    "coverage": "istanbul check-coverage --statements 100 --functions 100 --branches 100 --lines 100",
    "lint": "standard",
    "test": "node test.js"
  }
}
